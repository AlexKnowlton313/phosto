node_modules/
dist/
build/
cdk.out/
*.tsbuildinfo

# Local env / generated config
.env
.env.local
infra/.env
web/.env.local
web/public/config.json

# CloudFront signing keys — public PEM is committed-safe but keep both out by default
infra/keys/

# Lambda layer build output
functions/layers/*/nodejs/

.DS_Store
._*

# Local deployment config (contains account id)
infra/config.json

# Local preview fixtures (generated from a real card)

# CDK lookup cache (embeds the account id; keep out of a public repo)
infra/cdk.context.json
